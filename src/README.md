# src 目录说明

- **App.tsx** - 应用根组件
- **components/** - 通用 UI 组件
- **screens/** - 页面级组件
- **utils/** - 工具函数
- **constants/** - 常量、主题（如 `theme.ts`）
- **types/** - 全局 TypeScript 类型

从根目录引用示例：`import { colors } from './src/constants';`
